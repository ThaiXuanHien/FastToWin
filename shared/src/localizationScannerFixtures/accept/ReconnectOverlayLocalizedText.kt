import androidx.compose.material3.Text
import com.hienthai.fastowin.localization.TextKey
import com.hienthai.fastowin.localization.localized

fun reconnectOverlay() = Text(localized(TextKey.ReconnectingMatch))
